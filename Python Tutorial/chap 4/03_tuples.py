#creating a tuple using ()
t = (1,2,4,5)
# t1 = () #empty tuple
# t1 = (1) # Wrong way to declare a tuple with single elemnt
t1 = (1,) # declare single element in tuple
print(t1)

# Pritng the element of a tuple
# print(t[0])

## Cannot update the value of tuple
# t[0]= 34
